# M0 architecture, contracts, and supply-chain audit

- Reviewed commit: `ef3d476bc08f932f73a0417e5e5d59449cedfa09`
- Auditor task: `/root/audit_plan`
- Independent checks: clean worktree, matching HEAD, `git fsck`,
  `git show --check`, JSON parsing, AJV Draft 2020-12 compilation evidence,
  operation/event structural enumeration, official Arm checksum comparison,
  and OpenOCD tag/commit comparison.

## Approved evidence

- Shared architecture boundaries and injected side-effect interfaces are fixed.
- Human, JSON, and NDJSON contracts are versioned and stream-safe.
- All 14 operations have closed request and success-result schemas.
- All 15 events have closed, generation-scoped payload schemas.
- Debug lifecycle, guards, cleanup, cancellation, and reconnect behavior are
  decision-complete.
- Exit codes, typed errors, and single-use probe-bound authorization are fixed.
- The Microchip Studio `.cproj` subset, unsupported behavior, DFP handling, and
  vendor non-mutation guarantee are explicit.
- Generated writes are canonically confined to `.samdebug` with symlink escape
  rejection.
- The managed installer is fail-closed, user-local, verified, and atomic.
- Arm GNU Toolchain has an immutable artifact URL and official SHA-256; OpenOCD
  has a full upstream source commit.
- MIT project licensing, Rust dependency policy, GPL accompanying-source
  distribution, Microchip asset exclusion, and SPDX SBOM requirements are
  explicit.
- M1 redistributes no managed binaries and has no unresolved license decision.

APPROVED
