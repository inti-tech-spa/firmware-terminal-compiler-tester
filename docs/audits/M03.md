# M03 audit

Verdict: **REJECTED**

- Audited commit: `6c234a40983c94525ff1c76e908ff45789523f01`
- Audit task identity: `/root/audit_plan`
- Environment: macOS 26.5.2, Apple Silicon arm64
- Rust: rustc/cargo 1.97.1
- cargo-deny: 0.20.2
- cargo-audit: 0.22.2

## Passing evidence

- 44 clean locked workspace tests passed and two archive acceptance tests were
  ignored; all six importer tests passed.
- The real project imported for Debug and Release without changing its
  SHA-256, `26cab00bfc1905162fa0a0da3b3c21921db96ce268e7bb9f68024d54e6f38051`.
- Both real plans contained 35 sources and 13 headers with configuration flags,
  libraries, linker inputs, ASF/CMSIS paths, and artifacts captured.
- Fixture coverage, JSON initialization, formatting, strict Clippy,
  cargo-deny, and cargo-audit checks passed.

## Blocking findings

1. A pre-created `.samdebug` symlink redirected the generated plan outside the
   project.
2. Conditions on consumed items outside configuration property groups were not
   applied or rejected.
3. External Makefile elements and unknown expressions in raw flags were
   silently accepted.
4. Conflicting duplicate scalar settings silently selected the first value.
5. Some normalization and macro-expansion failures lacked XML locations.

## Required remediation

- Confine the state directory canonically and reject symlinks.
- Resolve or reject conditions on every consumed item and setting.
- Reject external build elements, item transforms, and unknown expressions in
  every consumed field.
- Reject conflicting scalar settings.
- Make every importer rejection location-aware and add regression tests.

M04 remains blocked until M03 is remediated and independently approved.

## Remediation audit 1

Verdict: **REJECTED**

- Audited commit: `035deafa2b5f5b7ed8c9bcb07b241b55e33dc37f`
- The original five blockers passed independent retesting.
- 46 tests passed and two archive acceptance tests were ignored; formatting,
  strict Clippy, cargo-deny, cargo-audit, and real-project nonmutation passed.
- A supported `$(Configuration)` macro in an include directory was expanded
  without the selected configuration.
- An unsafe library search path was rejected without XML-location details.

Required remediation is to pass project/configuration macro values through
include normalization and wrap library-search normalization failures at their
originating `Value` element.
