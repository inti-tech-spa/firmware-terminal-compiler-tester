# M05 audit

Verdict: **APPROVED**

- Audited commit: `e334df036a2544402cc1296cc405a145c9c5dcee`
- Comparison base for final remediation: `bd74d46838cec0e308d776d961ab323f340d621f`
- Audit task identity: `/root/audit_m4_remediation`
- Environment: macOS 26.5.2 arm64
- Rust: rustc/cargo 1.97.1
- OpenOCD: 0.12.0
- Arm GNU Toolchain: 15.2.Rel1, GCC 15.2.1, GDB 16.3.90,
  binutils 2.45.1
- cargo-deny: 0.20.2
- cargo-audit: 0.22.2

## Criteria and evidence

- Live discovery returned Atmel-ICE CMSIS-DAP serial `J42700028955`; doctor
  independently reported the powered target connected and verified all explicit
  tools. Exact selection precedes operation-and-serial authorization.
- OpenOCD uses direct argv, loopback-only dynamically selected ports with bounded
  fresh-set bind retries, CMSIS-DAP HID, SWD, `at91sam4sXX.cfg`, 1000 kHz, and
  owned process-group cleanup. Exhausted collisions return
  `LOCAL_PORT_UNAVAILABLE`/5.
- Probe transport diagnostics take precedence over port, verification, and
  operation fallbacks. Exact pinned CMSIS-DAP command failures, mismatch and
  protocol errors, USB/HID failures, and libusb bulk errors all return
  `PROBE_DISCONNECTED`/5. A combined transport-plus-bind regression makes no
  retry.
- Erase covers bank 0 and verifies erase state. Flash accepts only the managed
  project ELF, derives addresses from it, separately verifies the image, then
  resets to run. The public commands expose no raw address, arbitrary ELF,
  monitor command, or memory-write input.
- Destructive timeouts return `ERASE_TIMEOUT` or `FLASH_TIMEOUT`/6 and warn that
  target firmware state may be partial or unknown. Reset/halt timeout returns
  `TARGET_COMMAND_TIMEOUT`/5. Cancellation returns 130, kills and reaps the
  process group, and releases the probe.
- Formatting and strict all-target/all-feature Clippy passed. The locked
  workspace suite passed 70 tests with four explicit external/hardware ignores;
  all 10 M05 programming tests passed. cargo-deny passed all policy groups with
  configured warnings only. cargo-audit loaded 1,217 advisories, scanned 122
  dependencies, and found no vulnerabilities.
- The supplied physical acceptance passed erase, erase check, flash, independent
  verification, reset/run, halt/release, and readiness-based SIGINT/130. A final
  pass restored and verified the SUNSIGHT firmware. Independent cleanup checks
  found no OpenOCD/GDB process or listener.
- The isolated SUNSIGHT build compiled 33 sources, then reused all 33. Its ELF is
  ARMv7E-M EABI5 soft-float with entry `0x0040425d`, 23,096 flash bytes, and
  14,836 RAM bytes. The source clone stayed clean at `6866f330...`; its `.cproj`
  hash remained `8a334125c0103e8f8816c75d4db1449e1e587e594b09dce9b4c7dda4e79f1183`.

## Findings and remediation

No blocking findings remain. The first audit rejection is preserved in
`M05-attempt1.md`; the second in `M05-attempt2.md`. Both rounds were remediated
and independently re-audited.

M05 is approved. M06 may begin.
