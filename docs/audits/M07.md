# M07 — APPROVED

- Audit task identity: `/root/audit_m4_remediation`
- Approved candidate: `65e010457a408c2597f007e7433366b330ffdad9`
- Initial candidate: `c75a114f795587e027fa3ca372f0dbabaf31c082`
- Final remediation base: `39d5e59fcec491732baa24ba4112a2ee925b988c`
- Audit date: 2026-08-20, America/Santiago
- Verdict: **APPROVED**

## Criteria and evidence

- Ratatui provides source/disassembly, current execution location, stack, locals,
  registers, breakpoints, target output, logs, status, help, navigation, and a
  small-terminal fallback over the shared debug engine.
- Continue, halt, reset, firmware load, step, next, breakpoint management, and
  safe exit controls passed deterministic and physical checks. Firmware loading
  requires the exact `firmware.load:<probe serial>` authorization.
- JSON and NDJSON schemas compile and their examples validate. Agent mode is
  noninteractive, keeps stdout protocol-pure, bounds requests, rejects reused
  IDs, and publishes startup lifecycle events exactly once in order before the
  successful start response.
- Terminal cleanup passed normal-exit, partial-setup, panic-unwind, and real PTY
  Ctrl-C checks. Literal ETX is recognized before every input mode and before
  ordinary `c = continue`, cancels through the shared controller, exits 130,
  disables mouse capture, leaves the alternate screen, restores
  `icanon`/`isig`/`echo`, and reaps OpenOCD/GDB.
- The Apple Silicon release is a standalone Mach-O binary linked only to Apple
  system libraries/frameworks. Two locked builds produced identical hashes and
  clean-HOME/minimal-PATH acceptance passed without Rust or global mutation.
- SPDX 2.3 output passed `spdx-tools 0.8.3`. It contains 179 macOS-reachable
  packages, 357 relationships, three defined extracted `LicenseRef` entries,
  grouped compound license expressions, valid download locations, managed-tool
  binary/source records, and no Windows-only `winapi` packages.
- Release binary SHA-256:
  `e495cf8543d44c20a9616fb91a791b003bdd7908bc9e1b8c9e6eec9950697fda`.
- Release SBOM SHA-256:
  `a201655c4493c21fd7888ea9acc8c23cea252f52ecd81c3e2b156c050e6217e1`.
- Exact-candidate physical agent workflow passed start, breakpoint, continue,
  stop event, stack/register inspection, step, next, reset-halt, authorized
  managed firmware load with verification, stop, and cleanup.
- Independent exact-candidate physical PTY run reached Halted, then literal
  Ctrl-C returned 130 and left no samdebug, OpenOCD, GDB process, or listener.
- SUNSIGHT remained clean at
  `6866f3302a6386743396a5eaf15f9b25cc38c316`; its `.cproj` SHA-256 remained
  `8a334125c0103e8f8816c75d4db1449e1e587e594b09dce9b4c7dda4e79f1183`.

## Independent gates

- `cargo fmt --all -- --check`: passed.
- Locked workspace Clippy, all targets/features, `-D warnings`: passed.
- Locked workspace tests: 105 nonignored passed, 8 explicitly ignored.
- Focused TUI tests: 7/7 passed.
- macOS arm64 release build and acceptance: passed.
- Independent SPDX-aware validation: passed.
- Cargo-deny 0.20.2 advisories/bans/licenses/sources: passed with only configured
  duplicate and unused-license warnings.
- Cargo-audit 0.22.2: 1,217 advisories loaded, 187 dependencies scanned, no
  vulnerability finding.

## Environment and findings

- macOS 26.5.2 build 25F84, arm64.
- rustc/cargo 1.97.1.
- OpenOCD 0.12.0.
- Arm GNU Toolchain 15.2.Rel1; GDB 16.3.90.
- Blocking findings: none.
- Required remediation: none.

After the independent PTY run, a temporary inability to reopen the USB-visible
probe was attributed by kernel inspection to an unrelated Logitech Options+
HID client. No samdebug/OpenOCD/GDB ownership or leaked process existed, so this
was classified as external HID contention rather than a product finding.
